import pygame
import math

rad_angle = math.radians(0)  # 0
print(rad_angle)
rad_angle = math.radians(90)  # 1 // 2 * pi
print(rad_angle)
rad_angle = math.radians(180)  # 1 * pi
print(rad_angle)
rad_angle = math.radians(360)  # 2 * pi
print(rad_angle)

# 원의 둘레 = 2 * pi * r 이기 때문에 이렇게 된다.


print(pygame.font.get_fonts())
